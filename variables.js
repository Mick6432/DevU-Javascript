// Variables

// Variables can hold any data type.
// A variable that originally held a number
// can later in the program hold a string or
// some other data type.

let x = 7;
x = 6;
x = 5;
x = 4;
let y = 3;
let z =((x + y) + (x * y)) / (x - y);
console.log('Answer: ' + z);